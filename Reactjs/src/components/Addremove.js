import React from 'react';
//import {Options} from './Options'
export const Addremove=(props)=>{
     return (
         <div> 
<button onClick={props.show}>AddRemoves</button>
         </div>
     )
}